/* 
    Pages

    A collection of pages components that directly corresponds
    to app routes in Tanstack Router.
*/

import { EndpointTests } from './EndpointTests'

export const Pages = {
    EndpointTests
}